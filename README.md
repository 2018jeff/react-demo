"# react-demo" 

```bash
  > npm init
  > npm install webpack --save-dev
  > npm install webpack-dev-server --save-dev
  > npm install webpack-merge --save-dev
  > npm install html-webpack-plugin --save-dev
  > npm install clean-webpack-plugin --save-dev
  > npm install webpack-manifest-plugin --save-dev 
  > npm install workbox-webpack-plugin --save-dev  // google workbox 添加离线体验.
  > npm install http-server --save-dev  // 简易服务器，搭建出我们所需的离线体验,

  > npm install less less-loader style-loader css-loader --save-dev

  // react 需要的基础, 基于ES6语法.
  > npm install babel-loader --save-dev    // react jsx 语法所需.
  > npm install babel-cli babel-preset-react --save-dev 
  > echo '{ "presets": ["react"] }' > .babelrc

  > npm install react react-dom react-router redux react-redux axios --save  // react 库.


  > npm install antd-mobile --save

```